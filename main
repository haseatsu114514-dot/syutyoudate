// ============================================
// メイン処理ファイル - main.gs
// ============================================

/**
 * GETリクエスト処理（ヘルスチェック・Webhook検証用）
 */
function doGet(e) {
  console.log('=== doGet受信 ===');
  return ContentService.createTextOutput(JSON.stringify({ 
    status: 'ok', 
    message: 'LINE Bot is running',
    timestamp: new Date().toISOString()
  })).setMimeType(ContentService.MimeType.JSON);
}

/**
 * Webhook受信処理（LINE Messaging API）
 */
function doPost(e) {
  const startTime = Date.now();

  try {
    console.log('=== Webhook受信 ===');

    if (!e || !e.postData || !e.postData.contents) {
      console.error('Invalid request: no postData');
      return ContentService.createTextOutput(JSON.stringify({ status: 'error', message: 'Invalid request' }))
        .setMimeType(ContentService.MimeType.JSON);
    }

    const data = JSON.parse(e.postData.contents);

    if (!data.events || !Array.isArray(data.events)) {
      console.error('Invalid request: no events array');
      return ContentService.createTextOutput(JSON.stringify({ status: 'error', message: 'No events' }))
        .setMimeType(ContentService.MimeType.JSON);
    }

    console.log('受信イベント数:', data.events.length);

    data.events.forEach((event, index) => {
      try {
        console.log(`\nイベント${index + 1}/${data.events.length}を処理中...`);
        handleEvent(event);
      } catch (eventError) {
        console.error(`イベント${index + 1}の処理エラー:`, eventError);
        logError('イベント処理エラー', eventError.toString());
      }
    });

    const duration = Date.now() - startTime;
    console.log(`\n=== Webhook処理完了 (${duration}ms) ===`);

    return ContentService.createTextOutput(JSON.stringify({ status: 'ok' }))
      .setMimeType(ContentService.MimeType.JSON);

  } catch (error) {
    const duration = Date.now() - startTime;
    console.error(`doPost error (${duration}ms):`, error);
    logError('doPost error', error.toString());

    return ContentService.createTextOutput(JSON.stringify({ status: 'error', message: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

/**
 * イベントハンドラー
 */
function handleEvent(event) {
  console.log('イベントタイプ:', event.type);

  if (event.source && event.source.userId) {
    console.log('★★★ 受信したユーザーID:', event.source.userId, '★★★');
  }

  if (event.type === 'message' && event.message.type === 'text') {
    handleTextMessage(event);
  } else if (event.type === 'postback') {
    handlePostback(event);
  } else {
    console.log('未対応のイベントタイプ:', event.type);
  }
}

/**
 * テキストメッセージ処理（Reply API使用・無料）
 */
function handleTextMessage(event) {
  const userId = event.source.userId;
  const text = event.message.text.trim();
  const replyToken = event.replyToken;

  console.log('テキスト受信:', text);

  // replyTokenがない場合は処理しない
  if (!replyToken) {
    console.error('replyTokenがありません');
    return;
  }

  let replyMessages = [];

  // ID確認コマンド
  if (text === 'ID' || text === 'id' || text === 'ユーザーID' || text === 'マイID') {
    replyMessages = [{
      type: 'text',
      text: `あなたのユーザーID:\n${userId}`
    }];
    logAction('ID確認', userId, userId);
  }
  // 予約開始
  else if (text === '予約' || text === 'よやく') {
    replyMessages = [{
      type: 'template',
      altText: '予約フォーム',
      template: {
        type: 'buttons',
        text: '下記ボタンから予約フォームを開いてください\n\n※送信後、承認されるとLINEでお知らせします',
        actions: [
          {
            type: 'uri',
            label: '予約する',
            uri: buildBookingFormUrl(userId)
          }
        ]
      }
    }];
    logAction('予約開始', userId, '');
  }
  // 予約確認
  else if (text === '予約確認' || text === '予約一覧') {
    // 予約一覧はReply APIで返す
    const bookings = getUserBookings(userId);
    
    if (!bookings || bookings.length === 0) {
      replyMessages = [{
        type: 'text',
        text: '現在、予約はありません。'
      }];
    } else {
      let message = '【予約一覧】\n';
      bookings.slice(0, 3).forEach((booking, index) => {
        message += `\n${index + 1}. ${booking.desiredDateTime}\n   ${booking.course} - ${booking.status}`;
      });
      if (bookings.length > 3) {
        message += `\n\n他${bookings.length - 3}件`;
      }
      replyMessages = [{ type: 'text', text: message }];
    }
    logAction('予約確認', userId, `${bookings.length}件`);
  }
  // その他 - 応答しない（メッセージ節約）
  else {
    console.log('未対応キーワード - 応答スキップ:', text);
    return; // 応答しない = メッセージ消費なし
  }

  // Reply APIで返信（無料）
  replyLineMessage(replyToken, replyMessages, CONFIG.LINE_CHANNEL_ACCESS_TOKEN);
}

/**
 * 予約一覧表示
 */
function showUserBookings(userId) {
  try {
    const bookings = getUserBookings(userId);

    if (!bookings || bookings.length === 0) {
      sendLineMessage(userId, [{
        type: 'text',
        text: '現在、予約はありません。\n\n新規予約は「メニュー」から行えます。'
      }]);
      return;
    }

    let message = '【あなたの予約一覧】\n\n';

    bookings.forEach((booking, index) => {
      message += `${index + 1}. 受付番号: ${booking.requestId}\n`;
      message += `   日時: ${booking.desiredDateTime}\n`;
      message += `   コース: ${booking.course}\n`;
      message += `   状態: ${booking.status}\n`;

      if (booking.status === '承認済み' && booking.approvedDateTime) {
        message += `   確定日時: ${booking.approvedDateTime}\n`;
      }

      message += '\n';
    });

    sendLineMessage(userId, [{
      type: 'text',
      text: message
    }]);

    logAction('予約一覧表示', userId, `${bookings.length}件`);

  } catch (error) {
    console.error('showUserBookings error:', error);
    sendLineMessage(userId, [{
      type: 'text',
      text: '予約一覧の取得中にエラーが発生しました。'
    }]);
  }
}

/**
 * ポストバック処理（最速返信対応）
 */
function handlePostback(event) {
  const userId = event.source.userId;
  const data = event.postback.data;
  const replyToken = event.replyToken;

  console.log('=== ポストバック処理開始 ===');
  logAction('ポストバック受信', userId, data);

  if (!data) return;

  const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;

  // 承認処理（管理者）
  if (data.startsWith('approve_')) {
    const requestId = data.replace('approve_', '');

    // ★最速返信
    replyLineMessage(replyToken, [{
      type: 'text',
      text: `処理中...`
    }], adminToken);

    approveReservation(requestId, userId);
  }
  // お断り処理（管理者）
  else if (data.startsWith('reject_')) {
    const requestId = data.replace('reject_', '');

    // ★最速返信
    replyLineMessage(replyToken, [{
      type: 'text',
      text: `処理中...`
    }], adminToken);

    rejectReservation(requestId, userId);
  }
  // 日時変更処理（ユーザー - 弾かれ救済用）
  else if (data.startsWith('reschedule_')) {
    // reschedule_{requestId}_{datetime}
    const parts = data.replace('reschedule_', '').split('_');
    const requestId = parts[0];
    const newDateTime = parts.slice(1).join('_'); // 日時に_が含まれる可能性を考慮

    // ★最速返信
    replyLineMessage(replyToken, [{
      type: 'text',
      text: `${newDateTime} で予約リクエストを送信しています...`
    }]);

    handleReschedule(requestId, newDateTime, userId);
  }
  // 日時変更キャンセル（ユーザー）
  else if (data.startsWith('cancel_reschedule_')) {
    const requestId = data.replace('cancel_reschedule_', '');

    // ★最速返信
    replyLineMessage(replyToken, [{
      type: 'text',
      text: `予約リクエストをキャンセルしました。\n\n別の日時で再度お申し込みください。`
    }]);

    // 予約データを削除または「キャンセル」ステータスに更新
    handleRescheduleCancel(requestId, userId);
  }
}

/**
 * 日時変更処理（弾かれ救済用）
 * ユーザーがクイックリプライで新しい日時を選択した時の処理
 */
function handleReschedule(requestId, newDateTime, userId) {
  const startTime = Date.now();

  try {
    console.log('=== 日時変更処理開始 ===');
    console.log('受付番号:', requestId, '新日時:', newDateTime);

    const booking = getBooking(requestId);

    if (!booking) {
      console.error('予約が見つかりません');
      sendLineMessage(userId, [{
        type: 'text',
        text: `エラー: 予約が見つかりません。再度フォームからお申し込みください。`
      }]);
      return;
    }

    // 日時を更新
    booking.desiredDateTime = newDateTime;
    booking.status = '承認待ち';
    booking.updatedAt = new Date().toISOString();

    const saved = saveBooking(requestId, booking);

    if (!saved) {
      console.error('予約更新に失敗');
      sendLineMessage(userId, [{
        type: 'text',
        text: `エラー: 予約の更新に失敗しました。再度お試しください。`
      }]);
      return;
    }

    // 管理者に承認リクエストを送信
    const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
    const name = booking.userName || '（未設定）';

    const adminMessages = [
      {
        type: 'text',
        text: `新規予約リクエスト（日時変更）\n\n` +
              `━━━━━━━━━━━━━━\n` +
              `${name} 様\n` +
              `━━━━━━━━━━━━━━\n\n` +
              `📅 ${newDateTime}\n` +
              `💼 ${booking.course}\n\n` +
              `📞 ${booking.phone}\n` +
              `🏠 ${booking.postalCode}\n${booking.address}\n` +
              `━━━━━━━━━━━━━━\n` +
              `※ 元の希望日時: ${booking.originalDateTime || '不明'}`
      },
      {
        type: 'template',
        altText: '予約の確認',
        template: {
          type: 'confirm',
          text: `この予約を受け付けますか？`,
          actions: [
            {
              type: 'postback',
              label: '受け付ける',
              data: `approve_${requestId}`
            },
            {
              type: 'postback',
              label: 'お断りする',
              data: `reject_${requestId}`
            }
          ]
        }
      }
    ];

    const adminSent = sendLineMessage(CONFIG.ADMIN_USER_ID, adminMessages, adminToken);
    console.log('管理者通知送信結果:', adminSent ? '成功' : '失敗');

    logAction('日時変更リクエスト', userId, `${requestId} → ${newDateTime}`);

    const duration = Date.now() - startTime;
    console.log(`=== 日時変更処理完了 (${duration}ms) ===`);

  } catch (error) {
    console.error('handleReschedule error:', error);
    logError('日時変更処理エラー', error.toString());
    sendLineMessage(userId, [{
      type: 'text',
      text: `エラーが発生しました。再度フォームからお申し込みください。`
    }]);
  }
}

/**
 * 日時変更キャンセル処理
 */
function handleRescheduleCancel(requestId, userId) {
  try {
    console.log('=== 日時変更キャンセル処理 ===');
    console.log('受付番号:', requestId);

    const booking = getBooking(requestId);

    if (booking) {
      booking.status = 'キャンセル';
      booking.cancelledAt = new Date().toISOString();
      booking.updatedAt = new Date().toISOString();
      saveBooking(requestId, booking);
    }

    logAction('日時変更キャンセル', userId, requestId);

  } catch (error) {
    console.error('handleRescheduleCancel error:', error);
    logError('日時変更キャンセルエラー', error.toString());
  }
}

/**
 * 最速返信用の関数
 */
function replyLineMessage(replyToken, messages, token) {
  const url = 'https://api.line.me/v2/bot/message/reply';
  const accessToken = token || CONFIG.LINE_CHANNEL_ACCESS_TOKEN;
  
  UrlFetchApp.fetch(url, {
    'headers': {
      'Content-Type': 'application/json; charset=UTF-8',
      'Authorization': 'Bearer ' + accessToken,
    },
    'method': 'post',
    'payload': JSON.stringify({
      'replyToken': replyToken,
      'messages': messages,
    }),
    'muteHttpExceptions': true
  });
}

/**
 * 予約フォームURL生成（フォーム1回完結版）
 * プリフィル: 管理用ID(userId) + 郵便番号(〒)
 */
function buildBookingFormUrl(userId) {
  const formId = CONFIG.FORM.BOOKING.ID;
  const userIdEntry = CONFIG.FORM.BOOKING.USER_ID_ENTRY;
  const postalCodeEntry = CONFIG.FORM.BOOKING.POSTAL_CODE_ENTRY;

  return `https://docs.google.com/forms/d/e/${formId}/viewform?usp=pp_url&${userIdEntry}=${encodeURIComponent(userId)}&${postalCodeEntry}=%E3%80%92`;
}

/**
 * 予約承認処理（フォーム1回完結版）
 * 承認と同時にカレンダー登録＆確定通知を送信
 */
function approveReservation(requestId, adminUserId) {
  const startTime = Date.now();

  try {
    console.log('=== 予約承認処理開始 ===');
    console.log('受付番号:', requestId);

    const booking = getBooking(requestId);

    if (!booking) {
      console.error('予約が見つかりません');
      const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
      sendLineMessage(adminUserId, [{
        type: 'text',
        text: `エラー: 受付番号 ${requestId} の予約が見つかりません`
      }], adminToken);
      return;
    }

    if (booking.status === '確定') {
      console.log('すでに確定済みです');
      const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
      sendLineMessage(adminUserId, [{
        type: 'text',
        text: `この予約はすでに確定済みです\n\n受付番号: ${requestId}`
      }], adminToken);
      return;
    }

    // ステータスを「確定」に更新（承認と同時に確定）
    booking.status = '確定';
    booking.approvedDateTime = booking.desiredDateTime;
    booking.approvedAt = new Date().toISOString();
    booking.approvedBy = adminUserId;
    booking.updatedAt = new Date().toISOString();

    const saved = saveBooking(requestId, booking);

    if (!saved) {
      console.error('予約更新に失敗');
      const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
      sendLineMessage(adminUserId, [{
        type: 'text',
        text: `予約の更新に失敗しました\n\n受付番号: ${requestId}`
      }], adminToken);
      return;
    }

    // ★ Googleカレンダーに登録
    const name = booking.userName || '（未設定）';
    const calendarAdded = addToCalendar(booking.approvedDateTime, booking.course, requestId, name);
    console.log('カレンダー登録結果:', calendarAdded ? '成功' : '失敗');

    // ★ フォーム選択肢を更新（空き時間から削除）
    try {
      updateFormDateChoices();
    } catch (updateError) {
      console.error('フォーム選択肢更新エラー:', updateError);
    }

    console.log('承認・確定処理成功:', requestId);

    // ★ ユーザーへ確定通知（Push API - 有料1通目）
    const userMessage = `ご予約が確定しました\n\n` +
                       `━━━━━━━━━━━━━━\n` +
                       `📅 日時\n${booking.approvedDateTime}\n\n` +
                       `💼 コース\n${booking.course}\n\n` +
                       `🏠 当日お伺いする住所\n${booking.postalCode}\n${booking.address}\n` +
                       `━━━━━━━━━━━━━━\n\n` +
                       `${name} 様\n\n` +
                       `当日お伺いします\nご不明点はお気軽にお問い合わせください`;

    sendLineMessage(booking.userId, [{ type: 'text', text: userMessage }]);

    // ★ 管理者へ確定通知（Reply APIで既に返信済みなので、詳細はPushで送信）
    const adminMessage = `✅ 予約確定\n\n` +
                       `━━━━━━━━━━━━━━\n` +
                       `📅 日時\n${booking.approvedDateTime}\n\n` +
                       `💼 コース\n${booking.course}\n\n` +
                       `👤 お名前\n${name} 様\n` +
                       `━━━━━━━━━━━━━━\n\n` +
                       `📞 電話番号\n${booking.phone}\n\n` +
                       `🏠 住所\n${booking.postalCode}\n${booking.address}\n\n` +
                       `━━━━━━━━━━━━━━\n` +
                       `受付番号: ${requestId}\n` +
                       `カレンダー: ${calendarAdded ? '✓' : '✗'}`;

    const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
    sendLineMessage(adminUserId, [{ type: 'text', text: adminMessage }], adminToken);

    logAction('予約確定', adminUserId, requestId);

    const duration = Date.now() - startTime;
    console.log(`=== 予約承認・確定処理完了 (${duration}ms) ===`);

  } catch (error) {
    const duration = Date.now() - startTime;
    console.error(`approveReservation error (${duration}ms):`, error);
    logError('予約承認エラー', error.toString());
  }
}

/**
 * 予約お断り処理
 */
function rejectReservation(requestId, adminUserId) {
  const startTime = Date.now();

  try {
    console.log('=== 予約お断り処理開始 ===');
    console.log('受付番号:', requestId);

    const booking = getBooking(requestId);

    if (!booking) {
      console.error('予約が見つかりません');
      const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
      sendLineMessage(adminUserId, [{
        type: 'text',
        text: `エラー: 受付番号 ${requestId} の予約が見つかりません`
      }], adminToken);
      return;
    }

    if (booking.status === 'お断り') {
      console.log('すでにお断り済みです');
      const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
      sendLineMessage(adminUserId, [{
        type: 'text',
        text: `この予約はすでにお断り済みです\n\n受付番号: ${requestId}`
      }], adminToken);
      return;
    }

    // ステータス更新
    booking.status = 'お断り';
    booking.rejectedAt = new Date().toISOString();
    booking.rejectedBy = adminUserId;
    booking.updatedAt = new Date().toISOString();

    const saved = saveBooking(requestId, booking);

    if (!saved) {
      console.error('予約更新に失敗');
      const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
      sendLineMessage(adminUserId, [{
        type: 'text',
        text: `予約の更新に失敗しました\n\n受付番号: ${requestId}`
      }], adminToken);
      return;
    }

    console.log('お断り処理成功:', requestId);

    // ユーザーへ通知（管理botトークンで送信）
    const userMessage = `申し訳ございません\n\n以下の予約リクエストはお受けできませんでした\n\n受付番号\n${requestId}\n\n希望日時\n${booking.desiredDateTime}\n\nコース\n${booking.course}\n\n別の日時でお試しください`;

    sendLineMessage(booking.userId, [{
      type: 'text',
      text: userMessage
    }]);

    // 管理者へ確認通知
    const adminMessage = `お断り完了\n\n受付番号\n${requestId}\n\n日時\n${booking.desiredDateTime}\n\nコース\n${booking.course}\n\nユーザーに通知しました`;

    const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
    sendLineMessage(adminUserId, [{
      type: 'text',
      text: adminMessage
    }], adminToken);

    logAction('予約お断り', adminUserId, requestId);

    const duration = Date.now() - startTime;
    console.log(`=== 予約お断り処理完了 (${duration}ms) ===`);

  } catch (error) {
    const duration = Date.now() - startTime;
    console.error(`rejectReservation error (${duration}ms):`, error);
    logError('予約お断りエラー', error.toString());
  }
}

// ============================================
// Googleフォーム送信トリガー（統合版）
// ============================================

/**
 * フォーム送信時のトリガー関数（フォーム1回完結版）
 */
function onFormSubmit(e) {
  try {
    console.log('=== フォーム回答受信 ===');

    // フォーム1回完結なので、すべての回答を handleInitialFormSubmit で処理
    handleInitialFormSubmit(e);

  } catch (error) {
    console.error('onFormSubmit error:', error);
    logError('onFormSubmit エラー', error.toString());

    try {
      const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;
      sendLineMessage(CONFIG.ADMIN_USER_ID, [{
        type: 'text',
        text: `システムエラー\n\nフォーム送信処理でエラーが発生しました\n\nエラー: ${error.toString()}\n\n実行ログを確認してください`
      }], adminToken);
    } catch (notifyError) {
      console.error('エラー通知失敗:', notifyError);
    }
  }
}

/**
 * 予約フォーム送信処理（フォーム1回完結版）
 * 全情報を1回のフォームで取得し、承認後は即確定
 */
function handleInitialFormSubmit(e) {
  const startTime = Date.now();

  try {
    console.log('=== 予約フォーム処理開始 ===');

    const data = e.namedValues;

    // 基本情報
    const userId = data['管理用ID(⚠️変更を加えないでください)'] ? data['管理用ID(⚠️変更を加えないでください)'][0] : null;
    const name = data['お名前'] ? data['お名前'][0] : '（未入力）';
    const desiredDate = data['希望日'] ? data['希望日'][0] : null;
    const desiredTime = data['希望時刻'] ? data['希望時刻'][0] : null;
    const course = data['コース'] ? data['コース'][0] : null;

    // 連絡先情報
    const phone = data['電話番号'] ? data['電話番号'][0] : null;
    const postalCode = data['郵便番号'] ? data['郵便番号'][0] : '';
    const address = data['ご住所'] ? data['ご住所'][0] : null;

    console.log('受信データ - userId:', userId, 'date:', desiredDate, 'time:', desiredTime);
    console.log('連絡先情報 - phone:', phone ? '入力あり' : 'なし', 'address:', address ? '入力あり' : 'なし');

    if (!userId || !desiredDate || !desiredTime || !course || !phone || !address) {
      console.error('必須項目不足 - userId:', userId, 'date:', desiredDate, 'time:', desiredTime, 'course:', course, 'phone:', phone, 'address:', address);
      return;
    }

    // 日付フォーマットの整形（ドロップダウン "2/1(日)" 形式に対応）
    let dateStr = desiredDate;
    let reservationDate;

    if (desiredDate instanceof Date || typeof desiredDate === 'object') {
      reservationDate = new Date(desiredDate);
      dateStr = `${reservationDate.getFullYear()}-${String(reservationDate.getMonth() + 1).padStart(2, '0')}-${String(reservationDate.getDate()).padStart(2, '0')}`;
    } else {
      // "2/1(日)" 形式をパース
      const dateMatch = desiredDate.match(/^(\d{1,2})\/(\d{1,2})/);
      if (dateMatch) {
        const month = parseInt(dateMatch[1], 10);
        const day = parseInt(dateMatch[2], 10);
        const year = new Date().getFullYear();
        // 1月に12月の日付が来た場合は翌年として扱う
        const currentMonth = new Date().getMonth() + 1;
        const actualYear = (month < currentMonth && currentMonth >= 11) ? year + 1 : year;
        reservationDate = new Date(actualYear, month - 1, day);
        dateStr = `${actualYear}-${String(month).padStart(2, '0')}-${String(day).padStart(2, '0')}`;
      } else {
        // パースできない場合はそのまま
        reservationDate = new Date(desiredDate);
      }
    }

    // 時刻を追加
    const timeMatch = desiredTime.match(/(\d{1,2}):(\d{2})/);
    if (timeMatch && reservationDate) {
      reservationDate.setHours(parseInt(timeMatch[1], 10), parseInt(timeMatch[2], 10), 0, 0);
    }

    const desiredDateTime = `${dateStr} ${desiredTime}`;
    const requestId = generateRequestId();

    console.log('パース結果 - dateStr:', dateStr, 'reservationDate:', reservationDate);

    // ★★★ 高速カレンダーチェック ★★★
    const availability = checkCalendarFast(reservationDate, course);

    // 既に予約が入っている場合 → クイックリプライで空き時間を提案
    if (!availability.available) {
      console.log('カレンダーに予約あり - クイックリプライで救済');

      // 予約データを一時保存（日時調整中ステータス）
      const tempBookingData = {
        requestId: requestId,
        userId: userId,
        userName: name,
        desiredDateTime: desiredDateTime,
        course: course,
        phone: phone,
        postalCode: postalCode,
        address: address,
        status: '日時調整中',
        originalDateTime: desiredDateTime,
        createdAt: new Date().toISOString(),
        updatedAt: new Date().toISOString()
      };
      saveBooking(requestId, tempBookingData);

      // 空き時間を取得
      const suggestions = findAvailableSlots(reservationDate, course);

      if (suggestions.length > 0) {
        // クイックリプライで空き時間を提案
        const quickReplyItems = suggestions.slice(0, 10).map(slot => ({
          type: 'action',
          action: {
            type: 'postback',
            label: slot.replace(/^\d{4}-/, '').replace(' ', ' '),
            data: `reschedule_${requestId}_${slot}`
          }
        }));

        // キャンセルボタンも追加
        quickReplyItems.push({
          type: 'action',
          action: {
            type: 'postback',
            label: 'キャンセル',
            data: `cancel_reschedule_${requestId}`
          }
        });

        const rescheduleMessage = {
          type: 'text',
          text: `申し訳ございません。\n${availability.reason}\n\n以下の空き時間はいかがですか？`,
          quickReply: {
            items: quickReplyItems
          }
        };

        sendLineMessage(userId, [rescheduleMessage]);
        logAction('日時調整提案', userId, `${requestId} - 空き${suggestions.length}件`);
      } else {
        // 空き時間がない場合
        const noSlotMessage = `申し訳ございません。\n${availability.reason}\n\n本日の空き時間がありません。\n別の日で再度お申し込みください。`;
        sendLineMessage(userId, [{ type: 'text', text: noSlotMessage }]);
        logAction('予約リクエスト却下', userId, `${desiredDateTime} - 空きなし`);
      }

      const duration = Date.now() - startTime;
      console.log(`=== カレンダー重複 - クイックリプライ送信完了 (${duration}ms) ===`);
      return;
    }

    // カレンダーが空いている場合
    console.log('カレンダー空きあり - 予約リクエストを受け付け');

    // ★ユーザー通知は省略（Googleフォーム完了画面で確認してもらう）
    // → 1通節約 + 処理高速化

    // ★ 予約データ保存（全情報を含む）
    saveBooking(requestId, {
      requestId: requestId,
      userId: userId,
      userName: name,
      desiredDateTime: desiredDateTime,
      course: course,
      phone: phone,
      postalCode: postalCode,
      address: address,
      status: '承認待ち',
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString()
    });

    // ★ 管理者への通知（全情報を表示）
    const adminToken = CONFIG.ADMIN_BOT_TOKEN || null;

    const adminMessages = [
      {
        type: 'text',
        text: `新規予約リクエスト\n\n` +
              `━━━━━━━━━━━━━━\n` +
              `${name} 様\n` +
              `━━━━━━━━━━━━━━\n\n` +
              `📅 ${dateStr} ${desiredTime}\n` +
              `💼 ${course}\n\n` +
              `📞 ${phone}\n` +
              `🏠 ${postalCode}\n${address}\n` +
              `━━━━━━━━━━━━━━`
      },
      {
        type: 'template',
        altText: '予約の確認',
        template: {
          type: 'confirm',
          text: `この予約を受け付けますか？`,
          actions: [
            {
              type: 'postback',
              label: '受け付ける',
              data: `approve_${requestId}`
            },
            {
              type: 'postback',
              label: 'お断りする',
              data: `reject_${requestId}`
            }
          ]
        }
      }
    ];

    const adminSent = sendLineMessage(CONFIG.ADMIN_USER_ID, adminMessages, adminToken);
    console.log('管理者通知送信結果:', adminSent ? '成功' : '失敗');

    logAction('予約リクエスト受信', userId, requestId);

    const duration = Date.now() - startTime;
    console.log(`=== 予約フォーム処理完了 (${duration}ms) ===`);

  } catch (error) {
    console.error(`handleInitialFormSubmit error:`, error);
    logError('予約フォーム処理エラー', error.toString());
  }
}

// handleApprovedFormSubmit は廃止（フォーム1回完結のため）
